apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: auth-svc
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.auth.service.port }}
      targetPort: 8083
  type: {{ .Values.auth.service.type }}



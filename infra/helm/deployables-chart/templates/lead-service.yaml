apiVersion: v1
kind: Service
metadata:
  name: lead-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app.kubernetes.io/name: LeadsApp
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP



architecture: standalone

auth:
  enabled: true
  rootUser: root
  rootPassword: rootpassword
  usernames:
    - appuser
  passwords:
    - apppassword
  databases:
    - outreachdb

persistence:
  enabled: true
  size: 1Gi

service:
  nameOverride: mongo
  portName: mongodb
  port: 27017
